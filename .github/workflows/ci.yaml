name: ci

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

jobs:
  test:
    uses: ./.github/workflows/test.yaml

  build:
    needs: test
    uses: ./.github/workflows/build.yaml
